provider "aws" {
    access_key = ""
    secret_key = ""
    region = "us-east-1"
}

# Intentionally left empty, as we don't want to actually spin up resources.
# But, it lets us know if we setup/ran Terraform correctly.
